# Dietrix — Netlify safety net.
# AWS/Nginx production routing is defined in deploy/nginx/dietrix.conf.
# Unknown URLs must return the pre-rendered noindex error page with status 404.

/*    /404/index.html   404
